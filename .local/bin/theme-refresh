#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$XDG_CONFIG_HOME/themes/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  theme-set "$(cat $THEME_NAME_PATH)"
fi
